# Core service
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
httpx==0.28.1

# Face detection / recognition
insightface==0.7.3
onnxruntime==1.20.1

# Image / numeric
pillow==11.1.0
numpy==1.26.4

# Hashing (Ethereum keccak, not SHA3-256)
pycryptodome==3.21.0

# NOTE: insightface builds a native extension at install time. If the wheel
# build fails with a Cython/setup error, install build deps first:
#   uv pip install --python .venv/bin/python cython "setuptools<75"
# then re-run this requirements install.
